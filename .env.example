# CleanOps OpenEnv — Environment Variables
# Copy this file to .env and fill in values as needed.

# Optional: OpenAI API key for LLM-backed baseline agent
# Leave blank to use the default heuristic baseline (recommended for validation)
OPENAI_API_KEY=

# Server settings (optional overrides)
HOST=0.0.0.0
PORT=7860
